const { Client } = require('ssh2'); const db = require('./database'); async function run() { const hosts = await db.getSshHosts(); for(const h of hosts) { console.log('Connecting to ' + h.host); const conn = new Client(); conn.on('ready', () => { console.log('Connected.'); conn.exec('echo \'' + h.pass + '\' | sudo -S apt-get update && echo \'' + h.pass + '\' | sudo -S apt-get install -y smartmontools nvme-cli e2fsprogs sysstat', (err, stream) => { if (err) throw err; stream.on('close', (code, signal) => { console.log('Done with ' + h.host + ' Exit: ' + code); conn.end(); }).on('data', (data) => console.log('STDOUT: ' + data)).stderr.on('data', (data) => console.log('STDERR: ' + data)); }); }).connect({host: h.host, port: h.port, username: h.user, password: h.pass}); } } run();
